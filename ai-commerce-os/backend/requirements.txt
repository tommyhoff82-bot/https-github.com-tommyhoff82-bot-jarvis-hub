fastapi==0.110.0
uvicorn[standard]==0.27.1
prisma==0.14.0
python-dotenv==1.0.1
httpx==0.27.0
langchain==0.1.12
langchain-openai==0.0.8
langgraph==0.0.30
arq==0.26.3
redis==5.0.3
pydantic==2.6.3
stripe==9.0.0
pinecone-client==3.1.0
numpy==1.26.4
openai==1.12.0
# Hashing calls bcrypt directly (see auth.py) rather than going through
# passlib — passlib's own self-test crashes against modern bcrypt
# releases (confirmed against both 4.0.1 and 5.0.0 in this repo's testing:
# https://github.com/pyca/bcrypt/issues/684), and passlib hasn't had a
# release since 2020 to fix it.
bcrypt==4.2.1
pyjwt==2.8.0
email-validator==2.1.1
